package {{packageName}};

import org.springframework.web.bind.annotation.*;
import org.springframework.http.ResponseEntity;
{{#methods}}
{{#requestBody.isMultipath}}
import org.springframework.web.multipart.MultipartFile;
{{/requestBody.isMultipath}}
{{/methods}}
import java.util.*;

@RequestMapping
public interface {{name}} {

{{#methods}}
    /**
     * {{summary}}
     * {{description}}
     */
    @{{httpMethod}}Mapping("{{path}}")
    ResponseEntity<{{{returnType}}}> {{methodName}}(
        {{#params}}
        @{{#isPath}}PathVariable{{/isPath}}{{^isPath}}RequestParam{{/isPath}}(
           {{#required}} required = {{required}} {{#defaultValue}},{{/defaultValue}} {{/required}} {{#defaultValue}} defaultValue = "{{defaultValue}}"{{/defaultValue}}
        ) {{type}} {{name}}{{^isLast}},{{/isLast}}
        {{/params}}{{#requestBody}}{{#params}},{{/params}}
        {{#isMultipath}}
        @RequestPart MultipartFile {{name}}
        {{/isMultipath}}
        {{^isMultipath}}
        @RequestBody {{type}} {{name}}
        {{/isMultipath}}{{/requestBody}}
    );
{{/methods}}

}
